---
unix:
  children:
    macos:
      hosts:
        Wills-MBP:
          ansible_connection: local

    linux:
      children:
        physical_hosts:
        virtual_machines:

    physical_hosts:
      hosts:
        sbc1:
        sbc2:
        sbc4:
        nas1:
        kvm2:

    virtual_machines:
      children:
        # vm_workers:
        vm_pets:
        vm_dads:

    # vm_workers:
    #   hosts:
    #     vwkr[1:3]:

    vm_pets:
      hosts:
        vdb1:
        vpod:

    vm_dads:
      hosts:
        vws1:

    home:
      children:
        physical_hosts:
        virtual_machines:

    # k8s:
    #   children:
    #     vm_workers:
