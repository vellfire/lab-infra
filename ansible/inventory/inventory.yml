---
unix:
  children:
    macos:
      hosts:
        Wills-MBP:
          ansible_connection: local

    linux:
      children:
        physical_hosts:
        virtual_machines:

    physical_hosts:
      hosts:
        sbc1:
        nas1:
        kvm1:
        kvm2:

    virtual_machines:
      children:
        k8s_cluster:
        vm_pets:
        vm_dads:

    k8s_cluster:
      children:
        k8s_control:
        k8s_workers:

    k8s_control:
      hosts:
        k8s-control:

    k8s_workers:
      hosts:
        k8s-worker[1:4]:

    vm_pets:
      hosts:
        vdb1:
        vpod:
        wrk-test:

    vm_dads:
      hosts:
        vws1:

    seaweedfs:
      hosts:
        kvm1:
        kvm2:
        nas1:

    home:
      children:
        physical_hosts:
        virtual_machines:
