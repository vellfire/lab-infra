---
- name: Remove NetBird if installed
  ansible.builtin.dnf:
    name: netbird
    state: absent
    autoremove: true

- name: Remove NetBird repository
  community.general.dnf_config_manager:
    name: NetBird
    state: disabled
